# tshark Cheatsheet - Niveau OSI 1 (tshark)

## Description
Tshark est un analyseur de paquets en ligne de commande, version CLI de Wireshark.

# PRE_REQUIS_EXEC
apt update -y && apt install tshark -y

# COMMANDE_EXEC
tshark -i "$TARGET"